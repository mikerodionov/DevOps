sudo apt install azure-cli
# Check version
az version
# Upgrade in Windows Terminal, for Bash use packaage manager instead
az upgrade
az --help
# Install Bicep CLI
az bicep install
az bicep --help
az bicep version
az bicep upgrade
az bicep list-versions
